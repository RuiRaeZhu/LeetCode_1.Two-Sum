class Solution(object):
    def twoSum(self, nums, target):
        t = 0
        for t in range(len(nums)):
            s = t+1
            for s in range(len(nums)):
                if s>t and nums[t] + nums[s] == target:
                    return([t,s])
                    os._exit()
